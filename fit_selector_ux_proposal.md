# 🎨 가상피팅 핏감 3지선다 UI/UX 제안서

## 개요

가상피팅 실행 시 사용자가 **슬림핏 / 레귤러핏 / 오버핏** 중 원하는 핏감을 선택할 수 있는 UI 컴포넌트를 제안합니다.
현재 앱의 디자인 시스템(Charcoal Navy `#2F343A`, 라운드 카드, 미니멀 스타일)에 일관되게 맞춥니다.

---

## 목업 이미지

![핏감 선택 UI 목업](/Users/songjeonghun/.gemini/antigravity/brain/24556cb4-0d94-49fd-a283-ce0abc407d34/fit_selector_mockup_1774846510364.png)

---

## 디자인 안 비교

### 안 1: 카드형 셀렉터 ⭐ (추천)

목업 이미지에 해당하는 디자인입니다.

```
┌─────────────────────────────────────────┐
│  핏감 선택                               │
│                                         │
│  ┌─────┐   ┌══════┐   ┌─────┐          │
│  │  👕  │   ║  👕  ║   │  👕  │          │
│  │슬림핏│   ║레귤러║   │오버핏│          │
│  └─────┘   └══════┘   └─────┘          │
│  몸에 딱    적당한 여유   넉넉한 핏       │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │    ✨ 가상 피팅 시작하기          │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**특징:**
- 각 옵션이 **정사각형 카드**로 표시 (아이콘 + 텍스트)
- 선택된 카드: `PRIMARYCOLOR(#2F343A)` 배경 + 흰색 텍스트 + 그림자
- 비선택 카드: `#F8F9FA` 배경 + 얇은 보더 + 회색 텍스트
- 카드 아래 작은 설명 텍스트 (몸에 딱 맞는 / 적당한 여유 / 넉넉한 핏)

**장점:** 직관적, 터치 영역 넓음, 시각적 임팩트
**단점:** 세로 공간을 좀 더 차지함

---

### 안 2: 필(Pill) 셀렉터

```
┌─────────────────────────────────────────┐
│                                         │
│  핏감  ╭──────╮ ╭═══════╮ ╭──────╮     │
│        │슬림핏│ ║레귤러핏║ │오버핏│     │
│        ╰──────╯ ╰═══════╯ ╰──────╯     │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │    ✨ 가상 피팅 시작하기          │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**특징:**
- 상단 세그먼트 컨트롤(`_FittingRoomSegmentedControl`)과 동일한 스타일
- 3개의 필(pill) 버튼이 한 줄에 나란히
- 선택 시 부드러운 슬라이드 애니메이션 (배경색이 이동)
- 현재 앱의 "피팅룸 | AI 스타일리스트" 토글과 동일한 패턴

**장점:** 가장 컴팩트, 기존 UI 패턴과 일관성
**단점:** 아이콘 없이 텍스트만이라 직관성이 떨어질 수 있음

---

### 안 3: 비주얼 슬라이더형

```
┌─────────────────────────────────────────┐
│                                         │
│  핏감 선택                               │
│                                         │
│  슬림 ───●────────────────── 오버        │
│        ↑                                │
│     레귤러핏 (적당한 여유)                │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │    ✨ 가상 피팅 시작하기          │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**특징:**
- 슬라이더 바에 3개의 스냅 포인트 (슬림 / 레귤러 / 오버)
- 드래그로 핏감을 조절하는 느낌 → 패션앱에 잘 어울리는 인터랙션
- 선택된 핏감 아래에 설명 텍스트 표시

**장점:** 독특하고 감성적인 UX, "핏을 조절한다"는 메타포와 잘 맞음
**단점:** 3개 옵션에는 오히려 과한 느낌, 접근성 이슈

---

## 🏆 최종 추천: **안 1 (카드형)** + 안 2 (필형) 하이브리드

> 핏감이라는 시각적 개념을 전달하기 위해 **아이콘이 있는 카드형**이 가장 효과적이며,
> 공간 효율을 위해 카드 높이를 컴팩트하게 조절합니다.

## 배치 위치

현재 `_BottomCtaBar`의 **CTA 버튼("가상 피팅 시작하기") 바로 위**에 배치합니다.

```
현재 구조:
┌──────────────────────┐
│  헬퍼 텍스트           │  ← "상의를 선택하세요" 등
│  [가상 피팅 시작하기]   │  ← CTA 버튼
└──────────────────────┘

제안 구조:
┌──────────────────────┐
│  핏감 선택 (3지선다)   │  ← 새로 추가
│  [가상 피팅 시작하기]   │  ← CTA 버튼
└──────────────────────┘
```

> [!IMPORTANT]
> 핏감 선택은 **전신 사진 + 상의가 선택된 후(`isReady = true`)에만 표시**되어야 합니다.
> 아직 옷을 안 골랐을 때는 불필요한 선택지가 되므로 숨깁니다.

---

## 인터랙션 디자인

| 동작 | 효과 |
|------|------|
| 탭하여 선택 | 선택된 카드에 스케일 바운스 애니메이션 (`1.0 → 0.95 → 1.0`, 200ms) |
| 선택 전환 | 배경색 부드러운 전환 (`AnimatedContainer`, 250ms) |
| 기본값 | **레귤러핏** (가장 보편적인 선택) |
| 로딩 중 | 핏감 선택 영역 비활성화 (opacity 0.5) |
| 햅틱 피드백 | 선택 시 가벼운 햅틱 (`HapticFeedback.selectionClick`) |

## 아이콘 / 일러스트 아이디어

| 핏감 | 아이콘 | 설명 텍스트 |
|------|--------|------------|
| 슬림핏 | `Icons.compress` 또는 커스텀 SVG (타이트한 실루엣) | "몸에 딱 맞는" |
| 레귤러핏 | `Icons.straighten` 또는 커스텀 SVG (기본 실루엣) | "적당한 여유" |
| 오버핏 | `Icons.open_in_full` 또는 커스텀 SVG (넓은 실루엣) | "넉넉한 핏" |

---

## 컬러 스펙 (현재 앱 테마 기반)

| 요소 | 선택 상태 | 비선택 상태 |
|------|----------|------------|
| 카드 배경 | `#2F343A` (PRIMARYCOLOR) | `#F8F9FA` (INPUT_BG_COLOR) |
| 텍스트 | `#FFFFFF` (white) | `#565C66` (BODY_COLOR) |
| 보더 | 없음 | `#E5E7EB` (BORDER_COLOR), 1px |
| 그림자 | `PRIMARYCOLOR` 20% alpha | 없음 |
| 설명 텍스트 | `#2F343A` bold | `#8D949E` (MEDIUM_GREY) |

---

## 코드 구조 제안

```
lib/fitting/
├── component/
│   └── fit_type_selector.dart    ← 새로 만들 컴포넌트
├── model/
│   └── fit_type.dart             ← enum { slim, regular, oversize }
└── view/
    └── fitting_room_screen.dart  ← _BottomCtaBar에 통합
```

---

## 다음 단계

1. **핏감 옵션 3가지 확정** (슬림핏/레귤러핏/오버핏 이 맞는지?)
2. **디자인 안 선택** (카드형 / 필형 / 슬라이더형)
3. **백엔드 API에 핏감 파라미터 추가 필요 여부** 확인
4. 구현 착수
